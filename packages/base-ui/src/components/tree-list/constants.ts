import type { SelectionMode, SelectionBehavior, ListDensity } from './types';

export const DEFAULT_SELECTION_MODE: SelectionMode = 'single';
export const DEFAULT_SELECTION_BEHAVIOR: SelectionBehavior = 'replace';
export const DEFAULT_DENSITY: ListDensity = 'default';
export const DEFAULT_OVERSCAN = 10;
export const DEFAULT_ESTIMATED_ITEM_SIZE = 36;
export const DEFAULT_INDENTATION = 16;
export const TYPEAHEAD_TIMEOUT_MS = 500;
